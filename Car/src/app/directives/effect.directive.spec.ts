import { EffectDirective } from './effect.directive';

describe('EffectDirective', () => {
  it('should create an instance', () => {
    const directive = new EffectDirective();
    expect(directive).toBeTruthy();
  });
});
